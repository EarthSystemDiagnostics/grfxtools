% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/MapPlotly.R
\name{SaveMapPlotly}
\alias{SaveMapPlotly}
\title{SaveMapPlotly}
\usage{
SaveMapPlotly(p, file, width = 1024, height = 768, scale = 2)
}
\arguments{
\item{p}{a plotly object, e.g. the output of \code{\link{MapPlotly}}}

\item{file}{output file path; the extension controls the format
(\code{.png}, \code{.svg}, \code{.pdf}, \code{.webp})}

\item{width}{image width in pixels (default 1024)}

\item{height}{image height in pixels (default 768)}

\item{scale}{scale multiplier applied to \code{width} and \code{height};
use 2 for high-DPI PNG output (default 2)}
}
\description{
Save a plotly map widget as a static image (PNG, SVG, PDF,
  WebP). Requires kaleido to be installed; run \code{\link{SetupPlotlySave}}
  once if this is your first time saving plotly figures.
}
\examples{
\dontrun{
p <- MapPlotly(proj = "mollweide")
SaveMapPlotly(p, "map.png")
SaveMapPlotly(p, "map.svg", scale = 1)
}
}
